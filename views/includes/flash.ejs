<% errors.forEach(function(message){ %>
    <div class="flash flashError"><%= message %></div>
<% }) %>

<% success.forEach(function(message){ %>
    <div class="flash flashSuccess"><%= message %></div>
<% }) %>